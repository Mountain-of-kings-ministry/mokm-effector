strips should set boundries by their edge so they dont overlap in same track
if strips are overlaping in same track it should create a new track automaticaly for that strip above the current track
where the strip exists with same track type, so no overlaping occures.