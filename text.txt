layer 1 -> {
    [add support to drag one strip from one trak to another in same layer since thats what have been implimented]
    track 1 : strip1 strip2
    track 2 :      strip3 strip4
}

when spliting or duplicating it automatically creates a new track in the layer

layer 2 -> {
    [add support to drag one strip from one trak to another in same layer since thats what have been implimented]
    track 1 : strip1 strip2
    track 2 :      strip3 strip4
}